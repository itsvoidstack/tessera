/**
 * mock-data.ts — All example data has been removed.
 * The app now uses real user-entered data via AppStore (lib/store.tsx).
 * This file is kept as a stub to avoid breaking any stray imports.
 */

// Re-export store types for any legacy imports
export type { Project, Note, AuditIssue } from "@/lib/store";
